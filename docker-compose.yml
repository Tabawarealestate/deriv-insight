services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: deriv_insight
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - deriv_pg:/var/lib/postgresql/data
volumes:
  deriv_pg:
